export type filmInput = {
    filmId: { value: number };
    name: { value: string };
    year: number;
    price: { value: number };
    shortDescription: { value: string };
    description: { value: string };
};

export type idInput = {
    filmId: { value: string };
};
